spring:
  datasource:
    url: jdbc:mysql://localhost:3306/doreburn?createDatabaseIfNotExist=true
    username: admin
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
  data:
    redis:
      host: localhost
      port: 6379
      timeout: 10
      password: ""


logging.level.root: info