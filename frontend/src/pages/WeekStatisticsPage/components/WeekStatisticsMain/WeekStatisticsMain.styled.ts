import styled from 'styled-components';

export const WeekStatisticsMainContainer = styled.div`
  display: flex;
  flex-direction: column;
  gap: 3.2rem;
  overflow: hidden;
`;

export const WeekAchievementContainer = styled.div`
  flex: 1;
  overflow-y: auto;
  padding-bottom: 3rem;
`;
